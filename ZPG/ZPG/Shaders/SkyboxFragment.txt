#version 450

in vec3 FragPos;
in vec3 TextureCoo;
in vec3 Normal;


uniform samplerCube UISky;


out vec4 frag_colour;



void main () {
	 
    frag_colour = texture(UISky, TextureCoo);
	//frag_colour = vec4(1.0);
}

